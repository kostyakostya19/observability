{
    "title": "psql test table",
    "panels": [
      {
        "title": "test panel",
        "type": "table",
        "datasource": "test_psql",
        "targets": [
          {
            "rawSql": "SELECT * FROM users LIMIT 100",
            "format": "table",
            "refId": "A"
          }
        ],
        "gridPos": { "x": 0, "y": 0, "w": 24, "h": 10 }
      }
    ]
  }